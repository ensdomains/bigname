interface:
  display_name: "Phased Continuation"
  short_description: "Pick the next slice and hand it to orchestrator"
  default_prompt: "Use $phased-continuation to identify the next thin phased-development slice and route execution through $orchestrator."

policy:
  allow_implicit_invocation: true
