interface:
  display_name: "Verify Loop"
  short_description: "Run reviewer/fix cycles on the current diff"
  default_prompt: "Use $verify-loop to review the current work, fix real issues with failing tests, and repeat until clean."

policy:
  allow_implicit_invocation: false
