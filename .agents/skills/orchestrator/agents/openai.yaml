interface:
  display_name: "Orchestrator"
  short_description: "Coordinate execution through subagents"
  default_prompt: "Use $orchestrator to run this work in orchestration mode: classify the change, create bounded tasks, spawn subagents, steer them, and integrate the results without doing most implementation locally."

policy:
  allow_implicit_invocation: true
