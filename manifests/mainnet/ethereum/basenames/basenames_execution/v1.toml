# Shadow-scoped bootstrap for the canonical Basenames execution entrypoint on
# Ethereum mainnet before the promoted public route becomes active
# (upstream: .refs/basenames/README.md:L22 @ basenames@1809bbc).
manifest_version = 1
namespace = "basenames"
source_family = "basenames_execution"
chain = "ethereum-mainnet"
deployment_epoch = "basenames_v1"
rollout_status = "shadow"
normalizer_version = "ensip15@ens-normalize-0.1.1"
roots = []
discovery_rules = []

[capability_flags]
verified_resolution = { status = "shadow", notes = "shadow execution ownership is fixed before the promoted public Basenames verified-resolution manifest version becomes active" }

[[contracts]]
role = "l1_resolver"
address = "0xde9049636F4a1dfE0a64d1bFe3155C0A14C54F31"
proxy_kind = "none"
