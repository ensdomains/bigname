# Active public Basenames verified-resolution promotion for the frozen
# exact-surface transport-assisted direct-path class on Ethereum mainnet
# (upstream: .refs/basenames/README.md:L22 @ basenames@1809bbc).
manifest_version = 2
namespace = "basenames"
source_family = "basenames_execution"
chain = "ethereum-mainnet"
deployment_epoch = "basenames_v1"
rollout_status = "active"
normalizer_version = "ensip15@ens-normalize-0.1.0"
roots = []
discovery_rules = []

[capability_flags]
verified_resolution = { status = "supported", notes = "supports the frozen exact-surface transport-assisted direct-path public Basenames verified-resolution class" }

[[contracts]]
role = "l1_resolver"
address = "0xde9049636F4a1dfE0a64d1bFe3155C0A14C54F31"
proxy_kind = "none"
