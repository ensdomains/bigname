# Separate Sepolia hackathon candidate. Address/start provenance and qualifications:
# docs/manifests.md section "Sepolia hackathon deployment evidence".
# Source citations below establish ABI layouts only, not deployment addresses.
manifest_version = 1
namespace = "ens"
source_family = "ens_v2_registrar_l1"
chain = "ethereum-sepolia"
deployment_epoch = "ens_v2_sepolia_hackathon"
rollout_status = "active"
normalizer_version = "ensip15@ens-normalize-0.1.1"
discovery_rules = []

[capability_flags]
exact_name_profile = { status = "supported", notes = "selected hackathon ETHRegistry and ETHRegistrar supply exact-name facts; deployment validation remains separate" }
name_history = { status = "shadow", notes = "post-audit Sepolia registrar history remains downstream work" }

[[roots]]
name = "ETHRegistrar"
address = "0x7d1B7f586a62Ac3F54b9A396849757814283270b"
start_block = 11626738

[[contracts]]
role = "registrar"
address = "0x7d1B7f586a62Ac3F54b9A396849757814283270b"
proxy_kind = "none"
start_block = 11626738

# (upstream: .refs/ens_v2/contracts/src/registrar/interfaces/IETHRegistrar.sol:L32 @ ens_v2@a971bd64)
[[abi.events]]
name = "NameRegistered"
fragment = "event NameRegistered(uint256 indexed tokenId, string label, address owner, address subregistry, address resolver, uint64 duration, address paymentToken, bytes32 indexed referrer, uint256 base, uint256 premium)"
emitter_roles = [ "registrar" ]
normalized_events = [ "RegistrarNameRegistered", "PreimageObserved" ]

# (upstream: .refs/ens_v2/contracts/deployments/sepolia-20260629-r1/ETHRenewerV1.json:L110-L158 @ ens_v2@a971bd64)
[[abi.events]]
name = "NameRenewed"
fragment = "event NameRenewed(uint256 indexed tokenId, string label, uint64 duration, uint64 newExpiry, address paymentToken, bytes32 indexed referrer, uint256 amount)"
emitter_roles = [ "registrar" ]
normalized_events = [ "RegistrationRenewed", "PreimageObserved" ]
