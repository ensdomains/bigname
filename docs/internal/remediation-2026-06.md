# Remediation Plan — 2026-06 Full-Codebase Review

Internal tracking doc for the 2026-06-10 whole-repo review (reviewed at commit `602d3f1` on `fix/ens-v1-registry-owner-authority`, all subsystems, security explicitly out of scope; line numbers reference that commit). Findings are grouped into eight workstreams aligned with the ownership map in [`workstreams.md`](workstreams.md) and chosen to be file-disjoint so they can run in parallel. Each workstream is one branch, one PR chain, one owner.

The checklists below are condensed. The full subsystem review reports — including minor findings, named specifics behind the P2/P3 "bundle" items, reasoning chains, and verified-good areas — are archived in [`remediation-2026-06-appendix.md`](remediation-2026-06-appendix.md). Consult the owning report before working a bundle item.

Priorities: **P0** = production-breaking or merge-gating, **P1** = correctness gaps to schedule next, **P2** = important, **P3** = polish/perf. Evidence paths are repo-relative; line numbers are as of the review commit.

## Cross-stream rules

- **Migrations are serialized.** Any stream needing a migration declares it in its section below before writing it; WS-F owns migration review; timestamps are assigned at merge time, not authoring time.
- **Conformance fixtures are cross-workstream review points** (per `AGENTS.md`); new/changed fixtures get a second-stream reviewer.
- **Doc-first items** update the owning doc in the same PR (`AGENTS.md` rule). WS-H owns broad doc sweeps; other streams make targeted single-section edits only.
- **Shared `crates/storage` files**: WS-D owns `backfill_jobs/` and `lineage/` for the duration; WS-G owns `history.rs`; WS-A owns `normalized_events/upsert/repair/`; everything else storage-side is WS-F.
- One **integrator** watches the merge order and final consistency (per `workstreams.md` § High-Conflict Rules).

## Sequencing

1. **WS-H first** — cheap, no code conflicts, and the CI gates protect every other stream's landing.
2. **WS-A items 1–3 gate the `fix/ens-v1-registry-owner-authority` merge.**
3. All other streams start in parallel at will. WS-C item 1 (fuse masking) has no dependency on WS-A — the adapter already emits `PermissionScopeChanged`.

---

## WS-A · ENSv1 authority lifecycle (owner: Intake and Adapters)

Files: `crates/adapters/src/ens_v1_unwrapped_authority/**`, `crates/adapters/src/registry_migration_cache.rs`, `crates/storage/src/normalized_events/upsert/repair/**`. Items 1–3 block the current branch.

- [x] **P0 — Repair-SQL validation never filters (LEFT JOIN ON-clause).** All resource-anchor proofs in `crates/storage/src/normalized_events/upsert/repair/ens_v1_registry_event_time.rs:208-633` hang off `LEFT JOIN resources new_resource ON ...`; LEFT JOIN ON-predicates cannot remove rows, and the `updated` CTE repoints `resource_id` from `input.new_resource_id` regardless, so the fail-closed gate (`docs/storage.md` repair rules) cannot fire. Siblings (`ens_v1_renewal.rs`, `ens_v1_registry_event_time_null_resource.rs`) use INNER JOIN/WHERE correctly. Accept: validation in WHERE; failing test proving rejection of an orphaned/wrong-name/wrong-labelhash anchor. Closed in PR #15 (merged a878609).
- [x] **P0 — Decode registry `Transfer(bytes32,address)`.** Selected (`constants.rs:58`), suppression-checked, never decoded; `RegistryOwnerChanged` only comes from `NewOwner` (`observation.rs:129-155`). All `setOwner`/`setRecord` owner moves invisible, incl. NameWrapper wrap/unwrap registry handovers (upstream: .refs/ens_v1/contracts/registry/ENS.sol:L9 @ ens_v1@91c966f) (upstream: .refs/ens_v1/contracts/registry/ENSRegistry.sol:L63 @ ens_v1@91c966f) (upstream: .refs/ens_v1/contracts/registry/ENSRegistry.sol:L68 @ ens_v1@91c966f). Accept: namehash-targeted decode branch, migration suppression still applied. Closed in PR #15 (merged a878609).
- [x] **P0 — Unwrap fails to reactivate the prior registrar lease** (depends on the item above). Wrap demotes via the registry-owner-supersedes heuristic (`apply_registrar.rs:407-447`); unwrap restore compares stale `current_registry_owner` → wrap→unwrap binds registry-only instead of reactivating the registrar resource/lineage (violates `docs/architecture.md` rebind table). The owner-divergence rotation itself is an undocumented divergence — doc-first: document in `docs/upstream.md` or restrict. Accept: round-trip test reactivates prior resource + lineage. Closed in PR #15 (merged a878609).
- [ ] **P1 — Watch + decode NameWrapper `TransferBatch`** (`event_topics.rs:50-56` has `TransferSingle` only; subgraph handles batch (upstream: .refs/ens_subgraph/subgraph.yaml:L222 @ ens_subgraph@723f1b6) (upstream: .refs/ens_subgraph/src/nameWrapper.ts:L231 @ ens_subgraph@723f1b6) (upstream: .refs/ens_subgraph/src/nameWrapper.ts:L236 @ ens_subgraph@723f1b6)). Accept: per-id fan-out, multi-id test.
- [ ] **P1 — Clear stale wrapper authority on lapse + re-registration.** Nothing clears `current_wrapper_key` except observed `NameUnwrapped` (`apply_registrar.rs:303-305`); expired-past-grace + re-registered names keep the dead wrapper anchor. Accept: wrapper authority expires on lease release or fresh `RegistrationGranted` without same-tx `NameWrapped`.
- [ ] **P2 — Convergence-preload follow-up tests (from #15 adversarial review):** cross-batch refusal test (genuine-lapse lease reconstructed as superseded must NOT restore on a NewOwner matching the stale registrant, and must settle away — pins the release_ref reconstruction leg, registrar_state.rs:53-62); multiple-lease (lapse + re-registration) selection; cross-batch wrap-after-divergence through the preload; renewal-during-divergence on a preloaded superseded lease.
- [ ] **P2 — Basenames AuthorityTransferred owner Known↔Null parity (from #16 adversarial review):** the shared adapter emits AuthorityTransferred for basenames_base_registry with the same Option<&str>→JSON-null owner encoding (crates/adapters/src/ens_v1_unwrapped_authority/profiles.rs:30-34); the owner-transition rules widened on the #16 branch (repair legs + batch carve-out) are scoped to ens_v1_registry_l1/ethereum-mainnet, so the same Known↔Null replay divergence on base-mainnet hard-fails ingestion (fail-closed, loud). Accept: widen the scoped rules to basenames with tests, or document the exclusion. (upstream: .refs/basenames/src/L2/Registry.sol:L4 @ basenames@1809bbc) Note: the owner-preservation CASE is duplicated in ens_v1_registry_event_time_resource_id.sql:47-58 and ens_v1_registry_event_time_before_state.sql — widening the owner rules must update both copies (copy-drift risk flagged by the #16 fix-commit adversarial pass).
- [ ] **P2 — Grace-boundary off-by-one.** Release fires at `timestamp == expiry+90d`; upstream allows renewal at exactly that timestamp (upstream: .refs/ens_v1/contracts/ethregistrar/BaseRegistrarImplementation.sol:L161 @ ens_v1@91c966f). Accept: release strictly greater; boundary-renewal test.
- [ ] **P3 — Convergence-preload polish (from #15):** finalize_history doesn't drop a released superseded lease for names with zero applied observations (inert re-materialization — mirror the settle check); scope-0 registrar aggregation is a per-name full-history scan (perf note while diverged names are rare); preload_superseded_registrar_lease needlessly re-exported in preload.rs.
- [ ] **P3 — Robustness bundle:** reorged-away migration markers stay suppressed until restart (`registry_migration_cache.rs:131-166`); zero-length binding segments drop without `SurfaceUnbound` (`transition.rs:16-57`); `RegistrationGranted.before_state.registrant` always null (`apply_registrar.rs:54`); record events from non-current resolver dropped (`apply_resolver.rs:109-111`); two diverging migration-guard impls — share one.

## WS-B · ENSv2 + preimage coverage (owner: Intake and Adapters)

Files: `crates/adapters/src/ens_v2_*`, `crates/adapters/src/block_derived_normalized_events/**`, `crates/adapters/src/ens_v1_reverse_claim/`, taxonomy section of `docs/architecture.md`. Disjoint from WS-A modules.

- [x] **P1 — Consume ENSv2 registry/root `EACRolesChanged`.** Only the resolver family's role events are read (`ens_v2_permissions/load.rs:32`); registry/root EAC events — the core v2 permission surface — watched nowhere; `RootPermissionChanged` never emitted despite the taxonomy. Accept: registry EAC watched; `PermissionChanged`/`RootPermissionChanged` per docs. Closed in PR #22 (merged f29f48e): manifest admission + adapter emission with source-family role vocabularies; projection consumption tracked separately below.
- [x] **P1 — Consume RootPermissionChanged + registry-scope permission events in projections (from #22 split):** the adapter emits PermissionChanged (registry vocabulary) and RootPermissionChanged into normalized events, but permissions_current consumes PermissionChanged generically and nothing consumes RootPermissionChanged; v2 before_state carries {subject, role_bitmap, effective_powers} (diverges from v1's full shape — consumer must handle both). Owner: Projections. Accept: registry/root grants surface in permissions_current with the registry vocabulary; RootPermissionChanged consumed per taxonomy. Closed in PR #24 (merged 92da58f): permissions_current consumes both kinds with registry vocabulary; registry_root maps to Root scope; derive/invalidation widened; root-derivation provenance (inheritance_path registry_root_fallback) pinned by test.
- [ ] **P3 — Projection-consumption polish (from #24 review, owner Projections):** RootPermissionChanged revocation-to-empty untested (shared code path with the pinned v1 drop-revoked-rows case, low risk); `PermissionScope::Root` drops the scope object's chain_id/registry_address from the scope column (survives in inheritance_path/grant_source — add a doc sentence if multi-chain v2 roots land).
- [ ] **P1 — Pre-compose ENSv2 root fallback in name-level role reads (doc-first; user decision 2026-06-12, from #24 bot-comment triage):** GET /v1/names/{namespace}/{name}/roles returns only the resolved resource's permissions_current rows (apps/api/src/handlers/app_facing/roles.rs, roles_support.rs, crates/storage/src/permissions/paging.rs), while upstream hasRoles ORs root roles into resource-level checks (upstream: .refs/ens_v2/contracts/src/access-control/EnhancedAccessControl.sol:L181 @ ens_v2@554c309); current behavior is doc-aligned (docs/api-v1-routes.md "rows for that resource") but the owner wants root-derived powers composed into name-level responses. Accept: roles route includes the owning registry's root-resource grants for ENSv2 resources, distinguishable as root-derived (inheritance_path); docs/api-v1-routes.md roles section updated in the same PR; red test first. Owner: Projections/API.
- [x] **P1 — Preimage intake for post-2023 controller signatures.** `block_derived_normalized_events/constants.rs:21-44` covers legacy controller events only; wrapped/unwrapped controller variants and Basenames controllers feed nothing into `label_preimages`. Decode logic duplicated and already diverged (lost NUL check vs `preimage_observation.rs:9-11`). Accept: all admitted signatures produce observations; one shared decode helper. Closed in PR #22 (merged f29f48e).
- [x] **P2 — Taxonomy reconciliation (doc-first).** Never emitted: `RecordDeleted`, `CommitmentMade`, `DelegateRetainedAfterTransfer` (+ `RootPermissionChanged`, covered above). Emitted but undocumented: `RegistrarNameRegistered` (`ens_v2_registrar.rs:29`). ENSv2 ERC-1155 `TransferSingle/Batch` unwatched (owners never update post-registration; sepolia scope). Accept: each kind implemented or removed from the taxonomy with rationale. Closed in PR #22 (merged f29f48e).
- [x] **P3 — One malformed log fails the batch:** `ReverseClaimed` node mismatch `bail!`s the whole sync (`ens_v1_reverse_claim/events.rs:61-71`); same for malformed `AliasChanged` (`block_derived_normalized_events/event_builders.rs:199-210`). Drop/flag the single log instead. Closed in PR #22 (merged f29f48e).
- [ ] **P3 — WS-B robustness leftovers (from #22 review):** sibling malformed shapes still fail batches (NameWrapped, NamedResource family, Basenames NameForAddrChanged); dead sol! decls in event_builders.rs:39-45; mainnet controller manifest entries lack PreimageObserved normalized_events metadata; union-map topic matching in ens_v2_permissions vs per-manifest elsewhere; resolver-source root-resource disambiguation sentence when the projection contract lands.

## WS-C · Projection pipeline integrity (owner: Projections and API)

Files: `apps/worker/**`, projection-queue migrations (declare to WS-F: dead-letter state column/enum).

- [x] **P0 — Implement wrapper fuse masking of `effective_powers`.** Docs promise fail-closed fuse folding (`docs/api-v1-routes.md:460`, `docs/projections.md:115-119`); projection loads only `PermissionChanged` (`apps/worker/src/permissions/load.rs:82-86`) and copies powers verbatim; `PermissionScopeChanged` consumed nowhere. Found independently twice. Accept: latest canonical scope modifier applied; burned-fuse end-to-end test (also closes a test-matrix hole). Closed in PR #19 (merged 74ea76d). Masking applied consistently in permissions_current and address_names via one shared fail-closed helper; resolver/address_names invalidation fan-out included (two post-approval findings fixed pre-merge).
- [x] **P1 — Dead-letter handling.** `attempt_count` written, never read (`projection_apply/apply.rs:323`); a failing key retries forever every 60s and blocks primary hydration. Accept: max-attempt threshold → dead-letter state with operator visibility. *(Needs migration — coordinate with WS-F.)* Closed in PR #19 (merged 74ea76d).
- [x] **P1 — Manifest-driven invalidation producer.** Docs list manifest/capability changes as triggers; no producer enqueues them — projections reading manifest state at rebuild stay stale until an unrelated event lands. Accept: manifest sync enqueues affected family keys. Closed in PR #19 (merged 74ea76d).
- [x] **P1 — Close the multi-worker stale-overwrite window.** Generation bump clears the in-flight claim (`derive_queries.rs:52-55`), so a slow gen-G rebuild can publish last over gen-G+1 with the queue row gone; only the actively-applied row of a 25-row claim is heartbeated (`apply.rs:162-175`). Accept: per-key serialization + whole-batch heartbeat; concurrent-worker test. Closed in PR #19 (merged 74ea76d).
- [x] **P1 — Staged-replacement full rebuilds + marker hygiene.** Five families wipe-then-repopulate (permissions, children, record_inventory, resolver, primary_name), exposing empty reads and crash-truncated state that bootstrap markers never repair; per-family CLI commands skip the documented marker-clearing rule. `name_current`/`address_names` already correct. Accept: staged atomic publish everywhere; commands clear `current_projection_replay_status` first. Closed in PR #19 (merged 74ea76d).
- [ ] **P2 — ENSv2 `ParentChanged` → `children_current` invalidation.** Emitted with `logical_name_id = None` (`ens_v2_registry/events.rs:465-487`); both derive branches miss it while the rebuild consumes it. Accept: derive key from payload registry/parent fields.
- [ ] **P2 — Tooling: range rebuild, rewind, missing inspect subcommands** (docs promise point/range/full + rewind; only point/full exist, `cli.rs:230-522`; inspect lacks surface-binding, resolver-topology, declared-vs-verified diff). Implement or doc-first descope.
- [ ] **P3 — Efficiency bundle:** claim query defeats the pending partial index; per-task children block cache never hits (`children.rs:98-108`); inconsistent NULL `log_index` tie-breaking across four families (one shared sort key; also covers scope-modifier latest-selection: address_names orders NULL log_index last while permissions orders it first, divergent only for same-block synthetic scope events, none emitted today — flagged in #19 final review); duplicated derive loops; non-transactional upsert+stale-delete in point rebuilds; cross-family starvation needs age escalation.
- [x] **P3 — Database-scope the replay readiness index probe (from #16):** load_projection_replay_readiness counts pg_stat_progress_create_index cluster-wide with no datname filter (apps/worker/src/automatic_projection_replay.rs:466-470), so replay readiness blocks on unrelated databases' index builds (caused a CI worker-shard flake on #16; the handoff-check usage was removed in 42ca29d but the replay-path gate remains). Accept: filter to current_database(); flake-free readiness test. Closed in PR #19 (merged 74ea76d).
- [ ] **P3 — Dead-letter operational polish (from #19 review):** operator surface is the table alone (no log/metric/inspect subcommand); threshold (5) and retry delay hardcoded; history unpruned (one row per generation forever); fuse-clear events omitted from row provenance when no powers change; staging-connection pool headroom under tiny --database-max-connections. Accept: pick and implement the cheap subset, descope the rest explicitly.
- [ ] **P3 — Apply-lock hardening (from #19 review):** lock-connection death mid-apply silently lapses per-key serialization; blocked workers stall their whole claimed batch (no lock timeout); one fresh non-pool TCP connection per applied invalidation; CLI full rebuilds bypass per-key locks. Accept: bounded lock acquisition + lock-connection liveness check, or explicit descope.
- [ ] **P3 — Converge the two staged-publish mechanisms (from #19 review):** #16's NameCurrentReplacement (trigger-disable + sidecar rebuild) vs #19's five-times-included staged_rebuild.rs temp-table helper; same invariants, two implementations. Accept: one shared mechanism or documented rationale for two.

## WS-D · Intake resilience (owner: Intake and Adapters)

Files: `apps/indexer/**`, `crates/storage/src/backfill_jobs/**`, `crates/storage/src/lineage/**` (WS-D owns these storage files for the duration).

- [x] **P0 — Refresh backfill leases during range execution.** 300s lease never refreshed (`crates/storage/src/backfill_jobs/lease.rs:197-213`) vs 50k-block ranges; reservation prefers stealing expired ranges → `abort_all` kills bootstrap; crash-loops any deployment where a range exceeds 300s. Accept: `advance_backfill_range` extends the lease; concurrent-bootstrap test. Corroborating occurrence 2026-06-11: tests::bootstrap_auto_backfill_partitions_ranges_for_internal_workers failed under shard load on the #16 gate (lease-reservation timing; passed 5/5 solo and 3/3 full-shard rerun). Closed in PR #18 (merged 36ab81a). Root cause of the corroborated test flake identified and fixed in #18: concurrent completion of the last two ranges under READ COMMITTED left jobs stranded running; reservation now finalizes such jobs in-transaction.
- [x] **P0 — Reorg-during-backfill re-canonicalizes orphaned blocks.** Evidence loaded once per range; `chain_lineage_contains_ancestor` ignores canonicality (`lineage/reads.rs:182-235`); lineage upsert revives `orphaned → canonical` (`lineage/upserts.rs:127-128`). Accept: per-chunk evidence reload and/or ancestry proofs reject orphaned paths; mid-backfill reorg test. Closed in PR #18 (merged 36ab81a).
- [x] **P1 — RPC resilience.** Zero retry in the primary client (`provider/request.rs:177-221`), batch→sequential fallback amplifies 429s; log-range bisection recognizes one provider's limit message only (`logs_receipts.rs:480-483`); providers erroring on `safe`/`finalized` tags break head-following. Accept: bounded retry/backoff (model: the Coinbase client); multi-provider limit patterns; tag errors degrade to `None`. Closed in PR #18 (merged 36ab81a).
- [ ] **P1 — Unify the three materialization pipelines** (inline / sparse / Coinbase). Already drifted: sparse hardcodes code observations off (`fetching/sparse.rs:213`) and is the default bootstrap path → bootstrap history has no `raw_code_hashes`; live retains tx-sibling logs, backfill selected-emitter only (replay sees different fact sets per admission path). Accept: one stage implementation; identical retention/observation behavior (doc-first if intentionally narrowed). Deferred from #18 (drift verified real); stacked follow-up on a branch off main, owner WS-D.
- [ ] **P2 — Branch-proof safe/finalized head revival (from #18 review N1):** provider safe/finalized heads are written revival-capable without branch-membership proof (apps/indexer/src/main/reconciliation/canonical.rs:237,249) while checkpoints.rs:103-110 explicitly distrusts those heads for checkpoint advancement; a lagging provider can revive a just-orphaned block to a replay-eligible state. Accept: branch-membership validation before revival-capable head writes; test.
- [ ] **P3 — Align child payload-table canonicality CASEs with the preserve contract (from #18 review N2):** raw_children log/transaction/receipt and raw_code bulk upserts retain the orphaned→EXCLUDED revival branch while chain_lineage now preserves; transient parent/child skew reachable from backfill (fails closed for replay). Accept: shared or aligned CASE; test.
- [ ] **P3 — Provider-resilience refinements (from #18 bot review, supervisor-verified class):** mechanical guard for revival-capable upserts (clippy disallowed-methods scoped to reconciliation); unmatched throttle shapes (batch-level single-error throttles, eth_getBlockReceipts fallback amplification after exhausted batch retries) still reach the sequential path — extend code-based classification; degrade unsupported checkpoint-tag errors lacking echoed tag names where provider-verified.
- [ ] **P2 — Robustness bundle:** poll loop exits on transient DB errors (`runtime/poll_loop.rs:356-374`); unbounded non-checkpointed gap-fill up to 131k blocks (hand to bounded backfill machinery); no escalation past the reorg walk limit; one-block admission isn't one transaction; object-cache budget preflight hardcoded false; bootstrap serial-and-fail-fast with live intake.
- [ ] **P3 — RPC-cost bundle:** `eth_getCode` for the full watch plan per reconciled block (restrict to log-emitters); ops-catchup 32-block default chunks (~600k jobs full-history); inline mode full bundles where logs-first works; moving-tail chunk redone per follow iteration; per-row INSERT loop for event-silent observations; batch concurrency default 1.

## WS-E · Verified execution & primary names (owners: Verified Execution + Manifests and Discovery)

Files: `crates/execution/**`, `crates/manifests/**`, `manifests/**`, `apps/api/src/support/primary_name_lookup.rs` + primary-name response files (coordinate the API surface with WS-G; semantics owned here).

- [x] **P0 — Accept `https://` RPC endpoints.** `crates/execution/src/rpc.rs:80-84` bails on non-`http`; hosted providers are https-only, so live verified resolution and the ENS/60 fallback are unusable outside a local node. Also share the `reqwest::Client`. Closed in PR #17 (merged 5521105).
- [x] **P1 — Finish the verified-primary path.** No production caller for `persist_ens_verified_primary_name`; readback selects newest by request key only, ignoring chain positions/manifest versions/boundaries (`apps/api/src/support/primary_name_lookup.rs:78-104`); claim-change invalidation hooks are dead code → stale verified answers survive claim changes. Accept: claim-change invalidation wired; readback validates full cache identity; producer implemented or class documented backfill-fed. Partially closed in PR #17 (merged 5521105): producer decision documented backfill-fed (docs/execution.md), readback coin_type canonicalization landed. Remaining: claim-change invalidation wiring + full cache-identity readback — owner WS-E, sequenced after WS-C's merge (needs scoped exceptions into apps/worker/src/primary_name/projection.rs, crates/storage/src/primary_name/hooks.rs, apps/worker/src/execution.rs). Fully closed in PR #23 (merged 2d005fb): claim-change invalidation wired incl. hydration; full cache-identity write/read symmetry with post-upsert canonical validation; drift-as-miss readback.
- [ ] **P3 — Verified-primary polish (from #23 review):** unfenced stale-producer race (backfill persist after claim-change invalidation resurrects a stale outcome — anchor check is existence-only; document backfill quiescence or add a claim-content fence); drifted/legacy identity-less rows never evicted (warn-per-request; one-off cleanup); newest-row-only readback can mask an older valid row (scan-or-evict); test-stub SQL copy in apps/api/src/tests/support.rs.
- [ ] **P2 — Record-selector coin_type canonicalization (from #17 review N3):** `records=addr:060` vs `addr:60` create distinct execution-cache identities (apps/api/src/handlers query parsing admits digit-text verbatim, crates/storage/src/resolution_support/record_keys.rs:20-26,80-82 keeps it verbatim) — same distinct-key class #17 fixed for primary names. Owner WS-E (execution-cache identity semantics); files cross into WS-G/WS-F scope — scoped exceptions at execution time.
- [x] **P1 — EIP-3668 conformance.** GET chosen for `{sender}`-only templates and calldata never sent; `{sender}` not substituted on POST (`ens_resolution_ccip.rs:237-249`); 4xx retried against spec; `OffchainLookup`-required reverts persisted as cacheable `execution_failed` instead of documented `unsupported`; transient transport failures frozen into the cache (`ens_resolution_call.rs:100-117`). Closed in PR #17 (merged 5521105).
- [x] **P1 — API primary-names classification.** Route coverage keys off "persisted verified outcome present" instead of tuple-class membership (`apps/api/src/responses/resolution.rs:467-507`); in-class verified misses return `unsupported` where docs say `not_found`; `coin_type` not canonicalized (`"060"` forms a distinct tuple key and disables the ENS/60 fallback — same bug in `crates/execution/src/json_helpers.rs:79-95`); ENS/60 fallback returns `not_found` for unnormalizable claims where docs require `invalid_name`. Closed in PR #17 (merged 5521105).
- [ ] **P2 — Typed failure taxonomy + trace completeness.** Nearly everything collapses to `resolver_call_failed` (typed flags exist unused); `latency_ms` always null; ENS-side CCIP recorded only as digest, never as documented step rows; manifest-change cache invalidation is CLI-only — wire to manifest events.
- [ ] **P3 — Manifests polish:** version-tag vs `manifest_version` mismatch silently collides (`repository.rs:83-114`); duplicate contract roles unchecked; `normalizer_version` unvalidated; UR proxy declared `proxy_kind="none"` so upgrade-drift detection can't fire on the contract anchoring every verified answer; documented edge kinds (`parent`/`alias`/`metadata`/`transport`/discovered proxy) never produced; version bumps deactivate discovered edges until next replay; Basenames verified executor readback-only despite `supported` flag (doc-first decision).

## WS-F · Storage write-path & performance (owner: Storage and Domain)

Files: `crates/storage/src/identity/**`, trigger/index migrations. **Owns migration review for all streams.**

- [x] **P1 — `(chain_id, block_hash)` indexes for reorg orphaning.** Orphaning UPDATEs on `normalized_events` + four identity tables have no supporting index (`normalized_events/orphaning.rs:33-46`, `identity/orphan.rs:156-183`) — the most latency-critical write path seq-scans the largest tables. Accept: indexes added `CONCURRENTLY`, or predicates constrained by block range. Closed in PR #20 (merged dc83c79).
- [x] **P1 — Atomic merges on row-path identity writes.** Reload-merge-update without `FOR UPDATE` (`identity/write_rows/surface_binding.rs:71-117` + three sites in `write_rows.rs`) can re-extend tightened intervals or revive orphaned rows; bulk path already proves the merge in SQL. Accept: row paths adopt the bulk pattern or lock the reload. Closed in PR #20 (merged dc83c79).
- [ ] **P2 — Trigger/write-amplification bundle:** count/feed/invalidation triggers fire on read-equivalent `canonical→safe→finalized` promotions (gate on readability-class transitions); feed trigger lacks `WHEN`/`OF` (full per-address recompute on every projection rewrite, migration `20260521160000:476-479`); `projection_normalized_event_changes` has no pruning path; non-`CONCURRENTLY` index on `normalized_events` (`20260608160000`); four redundant indexes beyond the prior cleanup. Partially closed in PR #20 (merged dc83c79): count/feed sidecar triggers gated on readability-class transitions (canonical/safe/finalized promotions and observed↔orphaned no longer recompute); remaining: feed trigger WHEN/OF, projection_normalized_event_changes pruning, 20260608160000 non-CONCURRENTLY cleanup, redundant-index review.
- [x] **P2 — Repair policy: code-first.** ~14 ENSv1 repair migrations duplicate a ~150-line jsonb rewrite block, couple to `_sqlx_migrations.installed_on`/wall-clock cutoffs, three extend/reopen closed intervals against the tighten-only rule, three rewrite `event_identity` without the collision guards the Rust framework has. Accept: policy note (repairs land in the Rust framework); doc carve-out or correction for the interval rule. Closed in PR #20 (merged dc83c79).
- [ ] **P3 — Consistency bundle:** readable-universe predicate restated 6+ ways (call the existing SQL function everywhere); `change_kind='canonicality_update'` overloaded for payload repairs; `primary_names_current` missing documented projection-row metadata columns; execution-cache reorg invalidation loads every outcome into memory; history same-block ordering uses `transaction_hash` before `log_index`; per-event loop over a batch-capable upsert; `pg_trgm` installed but `contains` filters have no trgm index; raw_logs.emitting_address lowercase is a write-time convention with no CHECK/validation (raw_children/validation.rs:69-97) — #16 removed LOWER() from registry_migration_cache predicates on the verified invariant (reconciliation/payload.rs:340; coinbase rows.rs:410); add a CHECK constraint or intake validation so future intake paths cannot silently under-suppress migration markers (flagged independently by review N3 and the codex connector bot on #16).
- [ ] **P3 — Write-path hardening (from #20 review):** no deadlock/serialization-failure retry on batch row-path upserts (input-order FOR UPDATE locks); name the three historical interval-widening repair migrations in the docs/storage.md condemnation for traceability; add an observed→orphaned does-not-recompute gate test; CIC INVALID-index runbook note (drop-guards landed, runbook still worth a line).

## WS-G · API contract & pagination (owner: Projections and API)

Files: `apps/api/**` (minus primary-name semantics → WS-E), `crates/storage/src/history.rs`.

- [x] **P1 — SQL keyset pagination for `/v1/events` + `/v1/history/*`.** These load the entire filtered universe per request and paginate in memory (no LIMIT, `crates/storage/src/history.rs:95-244`; O(n) cursor scan) — the clearest scalability cliff. Gate `/v1/names`' unconditional `COUNT(*)` on `include=total_count` (`name_current/list.rs:187`). Closed in PR #21 (merged 89bd97a).
- [x] **P2 — Input-validation consistency.** Exact-name path segments skip ENSIP-15 (unnormalizable → 404, docs say 400, `handlers/exact_name.rs:3-27`); address validation differs across three route families; empty enum values inconsistent. One shared boundary, documented behavior for unnormalized input. Closed in PR #21 (merged 89bd97a).
- [ ] **P2 — Contract polish bundle:** `meta=full` and `include=record_summaries` accepted-but-inert on names/roles (implement or disclose + doc); hardcoded coverage on `/v1/addresses/{address}/names` (`responses/collections.rs:52-58`); `invalid_cursor` vs `invalid_input` doc inconsistency; undocumented `"stale"` from `/v1/status` + short-circuited degraded check; null-collision on unrequested compact-record sections; startup warm-up `bail!` → warning (`records_warmup.rs:31-42`). Partially advanced in #21: empty-enum defaults landed; invalid_cursor-vs-invalid_input vocabulary, meta=full/record_summaries inertness, /v1/status stale, and the rest remain.
  - Resolved in WS-H [#13](https://github.com/TateB/bigname/pull/13): declared-only provenance now omits `execution_trace_id` rather than serializing `null`.
  - records_warmup: #16 landed a narrower 404-only skip (NOT_FOUND sample → warn-and-continue); the broader bail!→warning generalization and the abandoned-warm-tasks early-return remain open.
- [ ] **P3 — Efficiency bundle:** N+1 `load_name_surface` in children compact view (`handlers/collections.rs:244-278`); O(n²) `Vec::contains` dedupe; per-request `JsonRpcHttpClient` in the primary-name fallback; sequential awaits that could join.
- [ ] **P3 — API polish (from #21 review):** execution_trace_id per-field COALESCE fallback differs from old whole-object provenance selection (summary.rs:106-136); resolver routes still use lenient normalize_address (shared strict boundary covers three families — extend or document); view=full history requests do O(universe) aggregate work per page by contract (the remaining scalability lever is a doc-first decision); wrapped-name route coverage test-matrix hole still unassigned.

## WS-H · Safety net & docs truth (owners: Platform and DevEx + Conformance and Fixtures)

Files: `.github/**`, `docker-compose*`, `docker/`, `.env*.example`, `docs/**` sweep, `tests/conformance/**`. **Land first.**

- [x] **P0 — Run the conformance suite in CI.** Closed on `fix/ws-h-safety-net-docs`: `.github/workflows/ci.yml` adds a full conformance job running `cargo test --manifest-path tests/conformance/Cargo.toml --locked` against the existing Postgres service.
- [x] **P0 — Restore the full conformance baseline to green.** Closed on `fix/ws-h-safety-net-docs`: full local conformance now passes with no filters, `continue-on-error`, or allowed-failure carve-outs; semantic corrections updated the owning docs/tests in the same change.
- [x] **P0 — Gate image publishing on CI; healthchecks.** Closed on `fix/ws-h-safety-net-docs`: `.github/workflows/docker.yml` publishes images only after successful CI on `main` as `latest` and short SHA; release-tag image publishing is deferred/not automatic rather than left as an ungated path; compose uses the API `/healthz` readiness endpoint and first-class indexer/worker `healthcheck` commands, with `public-proxy` gated on a healthy API.
- [ ] **P1 — Docs corrections sweep:** `docs/storage.md` partitioning + object storage describe nonexistent layers (remove dead MinIO services/env vars or implement); fuse-masking docs vs WS-C item 1 status; "TypeScript conformance harness" is Rust; conformance README describes a removed 400-requirement; `.env*.example` missing ~15 env vars the code reads; `crates/domain` ownership map vs reality (identity types live in `crates/storage/src/identity/types.rs`).
- [ ] **P2 — Test-matrix holes** (each test ideally lands with its owning stream; tracked here): fuse-changes-altering-control (WS-C), ENSv2 delegate-retained / shared-subregistry / alias-derived (WS-B), CCIP failure + offchain gateway mismatch (WS-E), Basenames primary unset (WS-E), wrapped-name route coverage (WS-G), proxy-implementation-change + manifest-version-change conformance (WS-E).
