# bigname keeps staging and commits explicit.
#
# No repo-local auto-approval rules are installed here. Agents inspect dirty state,
# stage explicit paths only when asked, and never stage unrelated work.
