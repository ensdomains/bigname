# Generated by scripts/check-rust-file-size --print-baseline.
# Tracks oversized hand-written production Rust files for the Phase 7 allowlist.

[settings]
warning_loc = 500
allowlist_required_loc = 600
review_required_loc = 900
new_file_max_loc = 1200
wiring_target_loc = 300
wiring_allowlist_required_loc = 500
wiring_files = ["lib.rs", "main.rs"]

[[files]]
path = "apps/api/src/graphql/name_queries.rs"
loc = 613
justification = "The Domain list builder keeps eligibility joins, generated-filter placement, and ordering in one query-shape boundary; this API slice adds only bounded planner-barrier selection through one shared production/test decision and should split the builder on its next substantive change."

[[files]]
path = "crates/manifests/src/schema_v2_persistence.rs"
loc = 607
justification = "Manifest state, address-epoch persistence, and derived-phase invalidation remain in one transaction boundary; PR #583 grew the file to 598 lines on main and this branch's nine-line no-effect notice crosses the threshold when rebased. Split on the next substantive touch."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v2_registry.rs"
loc = 663
justification = "ENSv2 registry dispatch keeps raw registration and reservation admission adjacent to retained topology refresh while expiry lifecycle helpers live in the bounded sibling module."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v2_registry/topology.rs"
loc = 685
justification = "ENSv2 topology removal and rebound groups remain adjacent while the fixed expiry event group and pointer clears live in the bounded sibling module."

[[files]]
path = "crates/adapters/src/schema_v2/identity.rs"
loc = 604
justification = "Name-surface materialization distinguishes active from shadow V1 surfaces so a later registry resolver can link only to a serving-visible name; the four-line active-surface notification keeps that identity decision at its operating boundary."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v1/registry.rs"
loc = 638
justification = "Registry event decoding and its atomic control/read transition remain together while shared owner classification and authority emission live in the bounded authority-transition sibling; the file remains below the review threshold."

[[files]]
path = "crates/adapters/src/schema_v2/state.rs"
loc = 606
justification = "The V1 interpreter keeps control state, literal registry owner history, and the resource retained for resolver reads together because restore and incremental interpretation must update them atomically; the file remains below the review threshold."

[[files]]
path = "crates/project/src/builders/name_authority.rs"
loc = 683
justification = "The exact-name authority selector keeps proof, released-state, source deployment period, and proven-zero-owner registry rules in one auditable query so known-absent authority cannot be confused with selector failure; the file remains below the review threshold."

[[files]]
path = "crates/project/src/builders/permissions.rs"
loc = 626
justification = "Effective permission folding keeps wrapper fuse masking, partial-coverage summaries, and the narrowly provenance-gated ENSv2 expiry tombstone in one resource-keyed projection boundary."

[[files]]
path = "crates/storage/src/record_inventory/snapshot_reads.rs"
loc = 612
justification = "Snapshot admission, anchor fallback, and canonical-lineage proof remain together; this slice ratchets the file below its base allowance without expanding its responsibilities."

[[files]]
path = "tools/benchmark-gate/src/api_load.rs"
loc = 930
justification = "The benchmark API orchestrator keeps target-identity boundaries, seed probes, timed sampling, and the single report contract together so a release gate cannot omit a validation phase."
review_justification = "Reviewed as non-serving release tooling: the file is below the 1200 LOC ceiling, its route workload builders and corpus loading are split into child modules, and its remaining code is one auditable gate sequence plus focused tests."

[[files]]
path = "tools/benchmark-gate/src/api_load/corpus.rs"
loc = 986
justification = "Production-scale corpus queries, their shared API-visibility filters, scale floors, namespace coverage, and database regressions remain together so the sampled and counted populations cannot drift apart."
review_justification = "Reviewed as non-serving release tooling: the file remains below the 1200 LOC ceiling, production query assembly is compact, and most of its size is database-backed coverage proving every sampled corpus family follows its serving-read visibility predicate."

[[files]]
path = "tools/benchmark-gate/src/api_load/corpus/resolver_coverage.rs"
loc = 634
justification = "Resolver manifest reconciliation, schema-v2 projection-phase admission, projected-row evidence binding, and the resulting operator failures remain together so the gate's expectation set is auditable as one query and verdict path."

[[files]]
path = "tools/benchmark-gate/src/api_load/preflight.rs"
loc = 621
justification = "API build, database, Interpret redo, Project publication, and manifest-authority snapshots remain one preflight and endpoint-boundary contract; focused database tests account for most of the excess over the normal file target."

[[files]]
path = "tools/benchmark-gate/src/api_load/probes.rs"
loc = 686
justification = "Untimed primary-name and per-endpoint seed evidence classifiers remain together so the release gate's populated-result and cursor requirements cannot diverge; most of the file is mutation-resistant refusal coverage."

[[files]]
path = "tools/benchmark-gate/src/database.rs"
loc = 1046
justification = "Read-only and disposable-copy connection preflights, per-connection instance pinning, marker authorization, and their database-backed safety tests remain co-located for one reviewable connection boundary."
review_justification = "Reviewed as non-serving release tooling: the file is below the 1200 LOC ceiling and most of its length is focused refusal-path coverage for the two connection modes rather than additional production responsibilities."

[[files]]
path = "tools/benchmark-gate/src/indexing.rs"
loc = 723
justification = "Index timing, rebuild, throughput, memory measurement, and red-report construction remain one release-gate operation with its budget and false-green regression tests."

[[files]]
path = "apps/phase-runner/src/redo_state.rs"
loc = 608
justification = "Redo marker state owns the atomic redo-begin transition and its authority/progress fencing. Discovery repair adds only the shared downstream-stamp call at that boundary; the stamping implementation was extracted to redo_stamp.rs, ratcheting this file down from 610 lines."
