# Generated by scripts/check-rust-file-size --print-baseline.
# Tracks oversized hand-written production Rust files for the Phase 7 allowlist.

[settings]
warning_loc = 500
allowlist_required_loc = 600
review_required_loc = 900
new_file_max_loc = 1200
wiring_target_loc = 300
wiring_allowlist_required_loc = 500
wiring_files = ["lib.rs", "main.rs"]

[[files]]
path = "apps/indexer/src/main/backfill/fetching/historical.rs"
loc = 805
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "apps/indexer/src/main/backfill/fetching/historical/log_payloads.rs"
loc = 653
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "apps/indexer/src/main/backfill/reservation_execution.rs"
loc = 615
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "apps/indexer/src/main/backfill/reservation_execution/coinbase_sql.rs"
loc = 790
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "apps/indexer/src/main/reconciliation/adapter_sync.rs"
loc = 677
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "apps/indexer/src/main/reconciliation/canonical.rs"
loc = 613
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/adapters/src/ens_v1_subregistry_discovery.rs"
loc = 602
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/adapters/src/ens_v1_subregistry_discovery/checkpoint.rs"
loc = 928
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."
review_justification = "Explicit review accepted this file above 900 LOC; future changes should split or shrink it."

[[files]]
path = "crates/adapters/src/ens_v1_unwrapped_authority/checkpoint.rs"
loc = 1109
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."
review_justification = "Explicit review accepted this file above 900 LOC; future changes should split or shrink it."

[[files]]
path = "crates/adapters/src/ens_v1_unwrapped_authority/loading/active_emitters.rs"
loc = 892
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/adapters/src/ens_v1_unwrapped_authority/materialization.rs"
loc = 634
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/manifests/src/lib/views/resolver_profiles.rs"
loc = 798
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/storage/src/base_normalized_rederive/batch/delete.rs"
loc = 1102
justification = "Base rederive delete batching keeps FK-safe step candidate SQL, trigger suppression, and batch delete SQL together for review during the live performance fix; future work should split the step SQL helpers after the run completes."
review_justification = "Explicit review accepted this file above 900 LOC for the live Base rederive performance fix because splitting the delete-step SQL during the maintenance window would broaden the change; future work should split or shrink it."

[[files]]
path = "crates/storage/src/identity/write_fast.rs"
loc = 790
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/storage/src/identity/write_fast/name_surface.rs"
loc = 633
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."

[[files]]
path = "crates/storage/src/label_preimages.rs"
loc = 641
justification = "Phase 7 allowlist for an oversized production file; future changes should split or shrink it."
