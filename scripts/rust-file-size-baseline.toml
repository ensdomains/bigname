# Generated by scripts/check-rust-file-size --print-baseline.
# Tracks oversized hand-written production Rust files for the Phase 7 allowlist.

[settings]
warning_loc = 500
allowlist_required_loc = 600
review_required_loc = 900
new_file_max_loc = 1200
wiring_target_loc = 300
wiring_allowlist_required_loc = 500
wiring_files = ["lib.rs", "main.rs"]

[[files]]
path = "apps/api/src/v2/events.rs"
loc = 774
justification = "History ergonomics (order, type sets, timestamp windows, include=data|raw, resolver filter) and the registry-route `contract_address` emitter filter keep the /v1/events handler, its cursor binding, and its filter parser adjacent; split the filter parser out on the next substantive touch."

[[files]]
path = "apps/api/src/v2/history.rs"
loc = 714
justification = "Name-history handler, the shared history read helpers (order, block window, capped total_count, cursor filter keys) and its cursor binding grew together; move the shared helpers into a sibling module on the next substantive touch."

[[files]]
path = "apps/api/src/v2/lookup/mod.rs"
loc = 609
justification = "Batch lookup keeps request parsing, name and address rendering, and served-head validation together; the registry-route subregistry pointer and the name-list authority/migrated_at fields land beside each other. Split on the next substantive touch."

[[files]]
path = "apps/api/src/v2/name_record.rs"
loc = 631
justification = "Name record read gained authority/migrated_at (F3), include=counts (F5), and the registry-route subregistry pointer in one handler; move the auxiliary loaders into a sibling module on the next substantive touch."

[[files]]
path = "apps/api/src/v2/namespaces.rs"
loc = 792
justification = "Namespace overview grew the per-chain verified-read capability report (F12) beside the existing overview assembly; split the capability report into a sibling module on the next substantive touch."

[[files]]
path = "apps/api/src/v2/params.rs"
loc = 902
justification = "Shared query-parameter grammar gained timestamp bounds, type sets, and the resolver selector (history ergonomics), contract_address (registry route), and expires_after/expires_before, sort_wire, and include_expired (name-list ergonomics) in parallel branches; split per-parameter parsers into a submodule now that the branches have landed."
review_justification = "Staging merge union of four parallel branches pushed the shared parameter grammar just past 900 LOC; accepted for the merge with the per-parameter parser split scheduled as the first follow-up."

[[files]]
path = "apps/api/src/v2/resolvers.rs"
loc = 610
justification = "Resolver overview route: snapshot selection, bound-name paging, support meta, and the overview assembly (including the declared ENSv1 mirror field); grant_event provenance now lives in resolvers/role_grants.rs."

[[files]]
path = "apps/api/src/v2/subnames.rs"
loc = 667
justification = "Subnames listing gained q/sort/order/include_expired (F7) and the registry-route subregistry pointer together with its cursor binding; split the cursor binding out on the next substantive touch."

[[files]]
path = "apps/api/src/v2/vocab.rs"
loc = 747
justification = "Product vocabulary gained HistoryEventTypeSet canonicalization (history ergonomics), Authority (name-list ergonomics), and the resolves_to relation in parallel branches; split history vocabulary into a submodule now that the branches have landed."

[[files]]
path = "apps/phase-runner/src/redo_state.rs"
loc = 608
justification = "Redo marker state owns the atomic redo-begin transition and its authority/progress fencing. Discovery repair adds only the shared downstream-stamp call at that boundary; the stamping implementation was extracted to redo_stamp.rs, ratcheting this file down from 610 lines."

[[files]]
path = "crates/adapters/src/schema_v2/identity.rs"
loc = 604
justification = "Name-surface materialization distinguishes active from shadow V1 surfaces so a later registry resolver can link only to a serving-visible name; the four-line active-surface notification keeps that identity decision at its operating boundary."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v1/registrar.rs"
loc = 614
justification = "The selected numeric registrar import keeps transfer authority convergence beside lifecycle dispatch; numeric ABI decoding and label enrichment are split into bounded child modules, while source10 migration correlation remains unchanged."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v1/registry.rs"
loc = 638
justification = "Registry event decoding and its atomic control/read transition remain together while shared owner classification and authority emission live in the bounded authority-transition sibling; the file remains below the review threshold."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v2_registry.rs"
loc = 663
justification = "ENSv2 registry dispatch keeps raw registration and reservation admission adjacent to retained topology refresh while expiry lifecycle helpers live in the bounded sibling module."

[[files]]
path = "crates/adapters/src/schema_v2/protocol/v2_registry/topology.rs"
loc = 685
justification = "ENSv2 topology removal and rebound groups remain adjacent while the fixed expiry event group and pointer clears live in the bounded sibling module."

[[files]]
path = "crates/adapters/src/schema_v2/state_surfaces.rs"
loc = 641
justification = "The selected numeric registrar import retains current authority selection and registry resolver-read state together; the new readable-name registrar evidence and snapshot eligibility are isolated in a bounded sibling module."

[[files]]
path = "crates/manifests/src/schema_v2_persistence.rs"
loc = 607
justification = "Manifest state, address-epoch persistence, and derived-phase invalidation remain in one transaction boundary; PR #583 grew the file to 598 lines on main and this branch's nine-line no-effect notice crosses the threshold when rebased. Split on the next substantive touch."

[[files]]
path = "crates/project/src/builders/record_inventory.rs"
loc = 849
justification = "The record inventory build stays one SQL derivation (pointer eligibility, node-keyed attribution, value selection, cleared-pointer history rows); the mirror-resolver pass lives in record_inventory/mirror.rs and this file only swaps mirror pointers for the substituted ENSv1 pointers and calls it."

[[files]]
path = "crates/storage/src/history.rs"
loc = 691
justification = "History read facade gained HistoryOrder, HistoryBlockWindow, HistoryPageOptions, the resolver filter, and the registry-route `contract_address` emitter filter while keeping every loader signature in one place; move the option types into a sibling module on the next substantive touch."

[[files]]
path = "crates/storage/src/history/paging.rs"
loc = 694
justification = "Keyset paging gained the reversible order predicate, per-chain block windows, and the resolver filter, which share one SQL builder; split the filter pushers from the cursor predicate on the next substantive touch."

[[files]]
path = "crates/storage/src/record_inventory/snapshot_reads.rs"
loc = 612
justification = "Snapshot admission, anchor fallback, and canonical-lineage proof remain together; this slice ratchets the file below its base allowance without expanding its responsibilities."

[[files]]
path = "tools/benchmark-gate/src/api_load.rs"
loc = 930
justification = "The benchmark API orchestrator keeps target-identity boundaries, seed probes, timed sampling, and the single report contract together so a release gate cannot omit a validation phase."
review_justification = "Reviewed as non-serving release tooling: the file is below the 1200 LOC ceiling, its route workload builders and corpus loading are split into child modules, and its remaining code is one auditable gate sequence plus focused tests."

[[files]]
path = "tools/benchmark-gate/src/api_load/corpus.rs"
loc = 986
justification = "Production-scale corpus queries, their shared API-visibility filters, scale floors, namespace coverage, and database regressions remain together so the sampled and counted populations cannot drift apart."
review_justification = "Reviewed as non-serving release tooling: the file remains below the 1200 LOC ceiling, production query assembly is compact, and most of its size is database-backed coverage proving every sampled corpus family follows its serving-read visibility predicate."

[[files]]
path = "tools/benchmark-gate/src/api_load/corpus/resolver_coverage.rs"
loc = 634
justification = "Resolver manifest reconciliation, schema-v2 projection-phase admission, projected-row evidence binding, and the resulting operator failures remain together so the gate's expectation set is auditable as one query and verdict path."

[[files]]
path = "tools/benchmark-gate/src/api_load/preflight.rs"
loc = 621
justification = "API build, database, Interpret redo, Project publication, and manifest-authority snapshots remain one preflight and endpoint-boundary contract; focused database tests account for most of the excess over the normal file target."

[[files]]
path = "tools/benchmark-gate/src/api_load/probes.rs"
loc = 686
justification = "Untimed primary-name and per-endpoint seed evidence classifiers remain together so the release gate's populated-result and cursor requirements cannot diverge; most of the file is mutation-resistant refusal coverage."

[[files]]
path = "tools/benchmark-gate/src/database.rs"
loc = 1046
justification = "Read-only and disposable-copy connection preflights, per-connection instance pinning, marker authorization, and their database-backed safety tests remain co-located for one reviewable connection boundary."
review_justification = "Reviewed as non-serving release tooling: the file is below the 1200 LOC ceiling and most of its length is focused refusal-path coverage for the two connection modes rather than additional production responsibilities."

[[files]]
path = "tools/benchmark-gate/src/indexing.rs"
loc = 723
justification = "Index timing, rebuild, throughput, memory measurement, and red-report construction remain one release-gate operation with its budget and false-green regression tests."

[[files]]
path = "apps/phase-runner/src/heads.rs"
loc = 616
justification = "Head-marker validation, retained provider finality, and publication checks share this module; database persistence is already split into heads/persist.rs. Preserve that boundary for this integration."

[[files]]
path = "crates/ingest/src/engine.rs"
loc = 636
justification = "Batch request types and orchestration share the ingestion entry point; live-follow, prefetch, query, redo, and source-floor logic already live in child modules. Preserve the existing orchestration boundary for this integration."

[[files]]
path = "crates/project/src/scope/topology.rs"
loc = 663
justification = "The topology closure keeps frontier setup and ancestor/descendant SQL together so their scope rules can be reviewed as one algorithm. This integration records the existing size without extending the algorithm."
